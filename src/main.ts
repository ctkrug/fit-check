const app = document.querySelector<HTMLDivElement>("#app");

if (app) {
  app.innerHTML = `
    <main>
      <h1>Fit Check</h1>
      <p>Scaffold running. UI lands in BUILD — see docs/BACKLOG.md.</p>
    </main>
  `;
}

export {};
