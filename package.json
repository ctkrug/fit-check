{
  "name": "fit-check",
  "version": "0.1.0",
  "description": "Instant GPU/model fit and speed check using real quantization math, not a lookup table.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ctkrug/fit-check.git"
  },
  "homepage": "https://github.com/ctkrug/fit-check#readme",
  "bugs": {
    "url": "https://github.com/ctkrug/fit-check/issues"
  },
  "keywords": ["llm", "gpu", "quantization", "gguf", "awq", "vram"],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
